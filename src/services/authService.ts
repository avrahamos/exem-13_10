import { authDto } from "../types/authDto";

export const loginService = async (authDto: authDto) => {
  try {
  } catch (err) {
    console.log(err);
  }
};

export const logoutService = async (authDto: authDto) => {
  try {
  } catch (err) {
    console.log(err);
  }
};
